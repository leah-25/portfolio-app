import Header from '../../components/layout/Header';

export default function Settings() {
  return (
    <div>
      <Header title="Settings" subtitle="Currency, API keys, export / import" />
      <div className="p-6">
        <p className="text-text-muted text-sm">Settings — coming soon.</p>
      </div>
    </div>
  );
}
